---
interface Props {
  scope: "post" | "site";
  viewKey?: string;
  label?: string;
  tone?: "inline" | "footer";
}

const {
  scope,
  viewKey = scope,
  label = scope === "site" ? "site views" : "views",
  tone = "inline",
} = Astro.props;
---

<span
  class:list={["view-counter", `view-counter-${tone}`]}
  data-view-counter
  data-view-scope={scope}
  data-view-key={viewKey}
  aria-label={label}
  title="目前先用這台瀏覽器的本機資料計算"
>
  <span class="view-mark" aria-hidden="true">◉</span>
  <span class="view-count" data-view-count>--</span>
  <span class="view-label">{label}</span>
</span>

<style>
  .view-counter {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    width: fit-content;
    font-family: var(--font-mono);
    font-weight: 800;
    line-height: 1;
    white-space: nowrap;
    vertical-align: middle;
  }
  .view-counter-inline {
    color: inherit;
  }
  .view-counter-footer {
    box-sizing: border-box;
    width: var(--footer-badge-width, 22rem);
    max-width: calc(100vw - 3rem);
    border: 3px solid var(--bg);
    background: var(--accent-lime);
    color: var(--text-on-lime);
    min-height: 4.5rem;
    padding: 0.9rem 1.4rem;
    gap: 0.7rem;
    font-size: clamp(1.1rem, 2vw, 1.55rem);
    border-width: 6px;
  }
  :global(.dark) .view-counter-footer {
    border-color: var(--ink);
  }
  .view-mark {
    display: inline-block;
    width: 1rem;
    height: 1rem;
    border: 2px solid var(--text-on-pink);
    border-radius: 999px;
    background: var(--accent-pink);
    box-shadow: 1.5px 1.5px 0 0 var(--text-on-pink);
    color: transparent;
    font-size: 0;
  }
  .view-count {
    min-width: 2ch;
    font-variant-numeric: tabular-nums;
  }
  .view-counter-footer .view-count {
    font-size: 1.05em;
  }
  .view-label {
    opacity: 0.74;
  }
  .view-counter-footer .view-label {
    opacity: 0.8;
  }
</style>
